jQuery(function()
{
	var container = jQuery('.container');
	
	var copy_buttons = continer.find('.share .button');
	
	if (container.is('.controller-coming_soon'))
	{
		copy_buttons.click(function()
		{
			var target = jQuery(this);
			
			
		});
	}
});